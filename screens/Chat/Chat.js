import React, {useState, useEffect} from 'react'
import {
    Text,
    View,
    Image,
    TouchableOpacity,
    SafeAreaView
} from 'react-native'

function Chat(props) {
    return <SafeAreaView>
        <Text>This is Chat Screen</Text>
    </SafeAreaView>
}

export default Chat